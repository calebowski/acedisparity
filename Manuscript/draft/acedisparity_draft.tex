\documentclass[11pt, a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{siunitx}
\usepackage[authoryear,round]{natbib}\usepackage{setspace}
\usepackage{lineno}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{multirow}
\setcitestyle{aysep={}}  % Removes comma between author and year

% Line spacing and numbering for review
\doublespacing
\linenumbers

\title{Disparity Analyses Are Robust To Ancestral State Estimation Uncertainty}

\author{Caleb Scutt\textsuperscript{1,*}, Natalie Cooper\textsuperscript{2}, Gavin Thomas\textsuperscript{1}, Thomas Guillerme\textsuperscript{1}}

\date{}

\begin{document}

\maketitle

\begin{center}
\small
\textsuperscript{1}School of Biosciences, University of Sheffield, Sheffield, UK\\
\textsuperscript{2}Natural History Museum, London, UK\\[12pt]
*Corresponding author: cnscutt1@sheffield.ac.uk
\end{center}

\begin{abstract}
\noindent
Ancestral state estimation is a widely used method to infer unobserved traits across a phylogeny, extending analyses beyond incomplete fossil records. 
In studies of morphological disparity ancestral state estimation can account for ghost lineages, yet estimates are often confounded with high uncertainty. 
How best to deal with this uncertainty remains unclear. 
Using simulations, we compared pre- and post-ordination estimation methods, as well as both point estimate and probabilistic approaches for treating uncertainty, under continuous and discrete trait models and varying fossil sampling levels.
The relative disparity error was calculated from the difference between our estimated disparity values and the true disparity values from the original simulations.
We then extended this framework to evaluate the recovery of changes in disparity across time by simulating mass extinction events with varying intensity and selectivity.
We find that pre-ordination probabilistic estimations outperform post-ordination and point estimation methods and that omission of ancestral state estimations leads to underestimates of disparity.
As expected, higher fossil sampling improves estimation accuracy, although recovered disparity accuracy was robust even at low fossil sampling. 
Similar patterns were observed when recovering changes in disparity across mass extinction events but declined significantly when fossil sampling was low, highlighting that fossil inclusion is essential when assessing disparity in deep time.
These results demonstrate that explicitly propagating uncertainty, rather than collapsing states into point estimates, is more likely to recover accurate patterns in disparity, reinforcing the need to move beyond point estimates when working with the incomplete fossil record.
\end{abstract}

\noindent\textbf{Keywords:} ancestral state estimation; morphological disparity; fossils; simulation; uncertainty; macroevolution

\section{Introduction}


\section{Materials and Methods}

\subsection{Simulation Framework}
\subsubsection{Phylogenies}
Simulations were carried out using \texttt{treats} \citep{guillerme2024treats}.
Phylogenies were simulated using a modified version of the birth-death process algorithm.
The process begins with a single lineage and iteratively selects lineages to grow the tree forward in time.
At each step, all living branches are extended by a branch length increment, drawn from an exponential distribution based on the total number of lineages and the combined speciation and extinction rates.
Selected lineages then undergo either speciation or extinction based on a probabilistic decision determined by the specified rates.
Phylogenies were simulated with birth-death parameters as speciation ($\lambda$) = 1.0, and extinction ($\mu$) across a range between 0.9-0.5.
Fixing $\lambda$ = 1.0 provides a convenient relative time-scale, since only the ratio of extinction to speciation affects the shape and balance of simulated trees.
The chosen parameters correspond to a moderate-to-high turnover regime, consistent with the upper-empirical estimates from both fossil- and molecular-based diversification studies of vertebrate groups. % add references here.
This level of extinction produces realistic evolutionary trees with substantial lineage pruning and extinct diversity, while maintaining a positive net diversification rate ($\lambda$ - $\mu$ = 0.3).
However, these parameters were not fixed, since they can vary if a tree fails to be generated with given stop rule and parameters.
A histogram of the distribution of speciation/extinction ratios is in the supplementary. % write this in
Trees were simulated at three different sizes: 50, 100 and 150 living taxa.




\subsubsection{Trait simulation}
Across each of the phylogenies, we simulated discrete and continuous trait data separately.
Discrete trait data were simulated across three rate categories, representing fast, medium and slow transition rates (table \ref{tab:models}).
These rates were assigned by generating equal rate transition probability matrices.
For each category, 85 binary characters and 15 three-state characters were simulated, representing the coding scheme found in most discrete datasets.
Discrete trait evolution was simulated using a discrete-time Markov process, where the probability of state transition along each branch was proportional to the product of the transition probability and branch length.
% trait simulations where traits were independent...

Continuous trait data were simulated under variations of Brownian motion and Ornstein-Uhlenbeck models (table \ref{tab:models}). 
For Brownian motion, the trait data was simulated as both default and with a moving trend.
For Ornstein Uhlenbeck, the data was simulated as strong, weak and with differing optimum and root states.
Following \cite{gearty2024impact}, the alpha ($\alpha$) parameter was set based on phylogenetic half life ($\mathit{ln(2)} / \alpha$) to account for differing tree heights across simulations \citep{cooper2016cautionary}.
Continuous trait data were simulated across 100 characters for each model across each phylogeny.
Each character was treated as independent.

\subsection{Fossil sampling}
In order to simulate different levels of fossil sampling, each tree was split into four chronological ‘stages’ to replicate the stratigraphic nature of the fossil record.
Fossil tips were sampled uniformly at random from all extinct lineages within each chronological stage.
% $\omega$ (where $\omega_{P} = P\% \ \text{fossils sampled}$).
Fossil sampling was carried out at five levels, $\omega_{0}, \ \omega_{5}, \ \omega_{15}, \ \omega_{50} \ \text{and}  \ \omega_{100}$ (where $\omega_{P} = P\% \ \text{fossils sampled}$), across each simulated phylogeny.
% Fossil tips were sampled uniformly at random from all extinct lineages within each chronological stage.
% Here i can reference a csv with all the tree tip and downsampled tree tip counts???
% Have thought here about expanding this: trait-dependent preservation (i.e more likely if in state 0 to be preserved??); fossil can be sampled at any point along its branch.
The corresponding trait matrices were subsetted to match sampled taxa, with ancestral (internal) nodes excluded.

\subsection{Ancestral state estimation}
\subsubsection{Pre-ordination estimation}
Ancestral state estimation was carried out using Maximum Likelihood.
We used four combinations of two approaches to ancestral state estimation to understand which was the best at recovering the true disparity signal.
Firstly, we compared pre- versus post-ordination estimation methods. 
Pre-ordination estimation methods of discrete characters involves estimating the marginal likelihoods for each discrete state at each node, based on a Maximum-Likelihood derived estimate of the Mk transition matrix and the tree topology given \citep{pagel1999maximum,yang2006computational}.
Given that the transition rates were set \textit{a priori} during simulations, the ER model was used to avoid model misspecification as a confounding factor on estimation accuracy \citep{keating2023best}.

Pre-ordination estimation of continuous characters models evolution under a Brownian motion model, whereby ancestral state values lie within a multivariate normal distribution that is estimated given the observed tip data and the shared evolutionary history between taxa, i.e., the tree topology. 
The width of the confidence interval is therefore usually dictated by the evolutionary distance from the tip data.
For a full explanation of the methods described here see \cite{revell2025ancestral}.

\subsubsection{Post-ordination estimation}
Post-ordination estimation involves the estimation of a node's position within a multidimensional ordination matrix.
The generation of an ordination matrix differed between discrete and continuous characters.
For discrete characters, distances were computed using the `MORD' metric \citep{lloyd2016estimating} and ordinated using principal coordinates analyses (PCoA) \texttt{cmdscale()} function.
For continuous characters, the tip data were ordinated using principal components analysis (PCA) \texttt{prcomp()}.
The continuous ancestral states across all PC axes were then estimated under a Brownian motion model, as detailed above.


\subsubsection{Uncertainty treatment}
For each estimation method, we also treated uncertainty in estimates with both deterministic and probabilistic approaches.
The deterministic approach involves collapsing the likelihood outputs into point estimates.
For discrete characters, this involved a strict majority rule whereby the state with the highest scaled likelihood was selected at each node for each character.
In scenarios where states are equally tied, one state is collapsed at random.
We also employed a relative majority rule, whereby any state at a scaled likelihood greater than the maximum observed scaled likelihood minus the inverse number of possible states is collapsed, otherwise states are left as uncertain.
For continuous characters, the deterministic approach used the median of the estimated ancestral confidence interval for each PC axis.

The probabilistic approach differs in that uncertainty in estimates are explicitly accounted for through the replication of matrices.
For instance, discrete states at each node for each character were `sampled' according to their scaled likelihoods across 100 matrices.
In a scenario where there is uncertainty in a binary character, e.g., 0.55 for state 0 and 0.45 for state 1, this probabilistic approach 



\subsection{Disparity metrics}
From the output of ancestral state estimation, to ensure fair comparison between different fossil sampling levels only the nodes present in each fossil sampling level were retained; therefore, fossil sampling levels differed based purely on the difference in number of fossil tips and estimations of node states.
Pre-ordination ancestral state estimations were ordinated in the same manner as before.
When assessing overall disparity, we wanted to understand how well ancestral state estimation can find true patterns in size and density in morphospace \citep{guillerme2020shifting}.
Since we are assessing overall disparity, position was not a metric that can be discenered due to the centering of ordinated matrices.
For size, we measured the sum of quantiles metric.
This metric has similar properties to the often used sum of ranges metric, but with less sensitivity to outliers.
For density, we measured the sum of variances and the average nearest neighbour distance.
The sum of variances is the most widely used metric in disparity analyses.
The relative disparity error was calculated as

\begin{equation}
\text{Relative disparity error} = \frac{\text{Estimated disparity} - \text{True disparity}}{\text{True disparity}}
\end{equation}


\subsection{Mass extinction simulations}
Having established the accuracy of different ancestral state estimation methods in recovering general morphospace dispersion, we next tested how well these methods capture changes in disparity across time during simulated mass extinction events.
Following the framework above, 100 trees were simulated across 50, 100 and 150 living taxa, but including random and selective extinction scenarios.
Extinction severity was set with a uniform distribution between 75--90\%.
For selective extinctions, % put parameters here.
%trait data was simulated simultaneously to the tree generation.

With the new set of trees (and trait data for selective extinctions), the pipeline %cite here
above was followed.





\begin{equation}
\text{Relative disparity error} = \frac{\text{Estimated disparity} - \text{True disparity}}{\text{True disparity}}
\end{equation}







\section{Results}

\subsection{Overall Performance of ASE Methods}



\section{Discussion}




\section{Conclusions}

% Pre-ordination probabilistic ancestral state estimation provides the most accurate approach for morphological disparity analysis. Explicitly accounting for uncertainty in ancestral reconstructions is crucial for robust disparity estimation, particularly when fossil sampling is limited.

\section*{Acknowledgments}


\section*{Author Contributions}



\section*{Data Availability}


\section*{Competing Interests}




\clearpage

\section*{Figures}

% \begin{figure}[h]
% \centering
% \includegraphics[width=0.8\textwidth]{figures/main-results.pdf}
% \caption{\textbf{Comparison of ancestral state estimation methods.} Relative disparity error for different ASE approaches across varying fossil sampling levels. Points show mean values with 95\% confidence intervals. Pre-ordination probabilistic methods (blue) consistently outperform alternatives.}
% \label{fig:main-results}
% \end{figure}

% \begin{figure}[h]
% \centering
% \includegraphics[width=0.8\textwidth]{figures/sampling-effects.pdf}
% \caption{\textbf{Effect of fossil sampling on disparity estimation.} Absolute relative error decreases with increased fossil sampling for all methods, but pre-ordination approaches remain most accurate even at low sampling levels.}
% \label{fig:sampling-effects}
% \end{figure}

\clearpage

\section*{Tables}

\begin{table}[ht]
\centering
\caption{Trait simulation models and parameters used across each phylogeny. For continuous characters, the root state was always set as 0.}
\label{tab:models}
\begin{tabular}{|c|c|c|}
\hline
\textbf{Type of data} & \textbf{Model} & \textbf{Parameters} \\
\hline
\multirow{3}{*}{\textbf{Discrete}} & Fast & $P_{ij}=0.9$ \\
\cline{2-3}
 & Medium & $P_{ij}=0.1$ \\
\cline{2-3}
 & Slow & $P_{ij}=0.01$ \\
\hline
\multirow{5}{*}{\textbf{Continuous}} & BM & $\sigma=0.25$ \\
\cline{2-3}
 & BM (trend) & $\sigma=0.25,\ \text{trend}=0.3$ \\
\cline{2-3}
 & OU (strong) & $\sigma=0.25,\ \alpha=\dfrac{\ln(2)}{\text{tree height / 10}}$ \\
\cline{2-3}
 & OU (weak) & $\sigma=0.25,\ \alpha=\dfrac{\ln(2)}{\text{tree height / 5}}$ \\
\cline{2-3}
 & OU (shift optimum) & $\sigma=0.25,\ \alpha=\dfrac{\ln(2)}{\text{tree height / 5}},\ \theta=2$ \\
\hline
\end{tabular}
\end{table}




\bibliographystyle{plainnat}
\bibliography{references.bib}

\end{document}